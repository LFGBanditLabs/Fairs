# Frontend (Next.js + Tailwind)

Quick scaffold for the frontend. To run locally:

1. cd frontend
2. npm install
3. npm run dev

The app runs at http://localhost:3000

Connect to your local Hardhat network or an externally deployed contract as needed.
